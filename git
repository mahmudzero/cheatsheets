# When wanting to use git@... instead of https, but certain tools
# use https cause they're just going off on their own
git config --global \
           --add url."git@github.com:".insteadOf "https://github.com/"
